[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && cd src/backend && npx prisma generate --schema=prisma/schema.prisma && npx prisma generate --schema=prisma/master/schema.prisma && npm run build"

[deploy]
startCommand = "cd src/backend && npx prisma migrate deploy --schema=prisma/master/schema.prisma && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
